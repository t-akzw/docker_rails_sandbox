<%= form_with(model: sample_user, local: true) do |form| %>
  <% if sample_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sample_user.errors.count, "error") %> prohibited this sample_user from being saved:</h2>

      <ul>
        <% sample_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
